class GenerateInventoryPage extends Component {
  state = {  }
  render() {
    return (
      <div>

      </div>
    );
  }
}


GenerateInventoryPage.propTypes = {
  
};

function mapStateToProps(state) {
  return {

  }
}

export default connect(mapStateToProps, {} )(GenerateInventoryPage);