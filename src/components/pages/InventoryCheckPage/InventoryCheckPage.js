class InventoryCheckPage extends Component {
  state = {  }
  render() {
    return (
      <div>

      </div>
    );
  }
}


InventoryCheckPage.propTypes = {
  
};

function mapStateToProps(state) {
  return {

  }
}

export default connect(mapStateToProps, {} )(InventoryCheckPage);